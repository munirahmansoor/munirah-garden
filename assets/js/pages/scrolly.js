// =============================================
// SCROLLY COMPONENT
// =============================================

function initScrolly() {

    const scrollyBlocks = document.querySelectorAll(".scrolly");

    if (scrollyBlocks.length === 0) return;

    console.log(`Found ${scrollyBlocks.length} scrolly component(s).`);

}